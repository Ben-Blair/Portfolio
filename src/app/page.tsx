import { Hero } from "@/components/hero/Hero";

/**
 * One viewport, nothing below it. Everything that used to live here — selected work, the
 * internships CTA — has a page of its own, reachable from the hero's pill row.
 */
export default function HomePage() {
  return <Hero />;
}
